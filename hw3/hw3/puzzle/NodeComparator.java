package hw3.puzzle;

import java.util.Comparator;
/**
 * Created by jhinukbarman on 3/20/17.
 */
/*

public class NodeComparator implements Comparator<T> {

    @Override
    public int compare(T o1, T o2) {
        return 1;
    }
    /*
    public int compare(WorldState o1, WorldState o2) {
        if (o1.estimatedDistanceToGoal() < o2.estimatedDistanceToGoal()) {
            return -1;
        } else if (o1.estimatedDistanceToGoal() > o2.estimatedDistanceToGoal()) {
            return 1;
        }
        return 0;
    }



    public boolean equals(T obj) {
        return true;
    }


}

*/
